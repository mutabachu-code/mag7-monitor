# Core - pin yfinance to latest stable with curl_cffi TLS support
# Yahoo Finance broke older versions in 2025 - must be >=0.2.37
yfinance>=0.2.40
curl_cffi>=0.7.0
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0

# Streamlit
streamlit>=1.35.0
streamlit-autorefresh>=1.0.1

# Claude API
anthropic>=0.25.0

# yfinance dependencies (explicit to avoid version conflicts)
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
peewee>=3.17.0
platformdirs>=4.0.0
multitasking>=0.0.11
protobuf>=4.21.0

# MetaTrader5 is Windows-only — only install on local Windows machine, NOT on Streamlit Cloud
# MetaTrader5
